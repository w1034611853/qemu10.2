---
id: kb-overview-current-state
title: 当前状态总览
note_type: overview
status: active
as_of: 2026-04-08
created: 2026-04-08
updated: 2026-04-08
owner: wangruoyu
audience:
  - self
  - handoff
scope: Human-readable normalized summary of the current NZCV optimization state as of 2026-04-08
summary: >
  用面向人的方式总结当前主线已完成内容、最新延伸提交、验证强度和仍然保守的边界。
tags:
  - current-state
  - nzcv
related_notes:
  - "[[当前已完成能力矩阵]]"
  - "[[当前风险、边界与未决问题]]"
  - "[[NZCV direct paths 与 producer chaining 当前状态]]"
related_commits:
  - 6d40860fef
  - 366b7d6ddb
  - 0c9d65605b
source_docs:
  - /home/wangruoyu/qemu10.2/progress.md
  - /home/wangruoyu/qemu10.2/performance_report/2026-04-03-nzcv-optimization-report.md
external_sources:
  - /home/wangruoyu/qemu_nzcv/functional_test_results.md
  - /home/wangruoyu/qemu_nzcv/performance_test_results.md
verification_status: partially-verified
verification_refs:
  - "[[TCG focused checks 总览]]"
  - "[[功能正确性总览]]"
  - "[[性能结果总览]]"
confidence: high
next_actions:
  - 把最新 chaining 扩展补入 progress.md 的 AI 运行态叙事
---

# 当前状态总览

## 一句话结论

截至 `2026-04-08`，这条 NZCV 优化线已经不再只是 compare-to-branch 的局部 fast path，而是形成了一个包含 direct-consumer、bounded-gap branch、compare-like non-branch 和 producer chaining 扩展的较完整实验主线；功能正确性证据整体较强，但性能收益明显依 workload 而分化。

## 当前可以认为“已站稳”的部分

- `ADCS/SBCS` direct-consumer 线已经完成并保持绿色
- `lazy compare phase 2` 的 `B.cond` 第一轮 rollout 已经形成可工作的 bounded-gap 模型
- compare-like non-branch support 已恢复，其中 `CSEL/CS*` 采用了 correctness-first 的 rework 模型
- focused TCG checks、`nzcv-status4`、`cmpstress-o3`、`adcsbc-bench` 和 `531.deepsjeng_r test`
  都已被记录为通过
- 外部功能正确性报告覆盖到 CoreMark、Dhrystone 和多项 SPEC CPU2017 `test` workload

## 需要显式看到的“更新到当前头”的部分

`progress.md` 的主叙事主要停留在 `6d40860fef` 附近，但当前分支在它之后还有两笔重要代码提交：

- `366b7d6ddb` `aarch64: extend NZCV direct paths and producer chaining`
- `0c9d65605b` `aarch64: extend NZCV logic and CCMP producer chains`

从提交标题、改动文件和新增 focused harness 来看，可以合理判断：

- direct-path 已经继续扩展到更多 chaining 形态
- logic / CSEL / CCMP / B.cond 的链式组合已被纳入新的 focused host-direct checker
- 当前“项目状态”不能再只理解为 `progress.md` 中那一轮 merge 之后的静态结果

这里的细节解读见 [[NZCV direct paths 与 producer chaining 当前状态]]。

## 当前最重要的结构性认识

### 1. correctness 依然是第一约束

最典型的证据就是 compare-like `CSEL/CS*` 的路径：先前的 `peek + keep alive`
模型不是“局部有 bug”，而是生命周期边界本身不够稳。当前路线已经明确偏向：

- direct 条件消费可以保留
- 但 consumer 之后的 flags 可见性必须尽快回到稳定表示

### 2. current state 是“保守扩展”，不是“全面 lazy flags”

虽然 direct-path 和 chaining 范围在扩大，但设计仍然明显保守：

- bounded gap
- whitelist
- invalidation-first
- 一次只扩大一个 consumer family 或一类 chaining 形态

### 3. performance 不能简单按“路径覆盖率”判断

外部报告显示：

- `505.mcf_r` 有明显收益
- `502.gcc_r` 基本持平
- `531.deepsjeng_r`、`541.leela_r` 存在回退

所以当前状态应该解读为：

- correctness closure 已经相当扎实
- performance story 仍然是 workload-dependent

## 当前建议如何理解“做到哪了”

如果是为了后续继续开发，可以把当前状态粗分为三层：

### 稳定层

- 已有 focused tests 和更宽 workload 结果支撑的主线能力

### 观察层

- 最近两笔 chaining 扩展已经进入主分支，但还需要更多知识化整理和统一叙事

### 规划层

- 下一步更像是在做 selective expansion、performance refinement 和路线选择
- 而不是从零搭基本机制

## 读取边界

这篇笔记是对截至 `2026-04-08` 的“人类读法”归一化总结，不是机器接手时的实时执行入口。若要继续动代码，请同时参考 `progress.md`。
